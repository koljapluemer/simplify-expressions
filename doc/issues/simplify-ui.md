let's simpliy the UI.
Get rid of the 6-times redundant headings, and labels, and badges noone asked for.

Show ONE!!!! instruction/feedback, the original term, the input field, and ONE!!! button.

When nothing is in the input field show "Give up & Show Solution" (and if clicked act accordingly), if something in the input field show "Check Solution" (and act accordingly). Model this as a clean state machine.